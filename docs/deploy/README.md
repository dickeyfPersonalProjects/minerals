# Deploying minerals with Flux (k3s + GitOps)

This guide documents the pattern for deploying the minerals app to a
k3s cluster via Flux. It is paired with a complete, well-commented
example overlay under [`example/`](./example/) that you can copy into
your fleet-infra GitOps repo and adapt.

The example reflects the contract between this repo's `kustomize/base/`
and a per-env GitOps overlay. The intent is that someone setting up a
new environment can read this README, copy `example/staging/`, change
the hostname/namespace, regenerate the SealedSecrets, and have a
working deployment.

> **Pairs with** [`encrypt.md`](./encrypt.md) for the kubeseal workflow
> used by the SealedSecret manifests in the example, and
> [`secrets.md`](./secrets.md) for the inventory of every Secret the
> deployment consumes.

---

## The base/overlay split

Two repos, two responsibilities:

| Concern                         | Lives in                                  |
|---------------------------------|-------------------------------------------|
| **What to run** (app, DB, MinIO, ConfigMap defaults, Service, resource sizing) | This repo: [`kustomize/base/`](../../kustomize/base) |
| **How it runs in env X** (namespace, image tag, ENV-specific config patches, secrets, ingress, TLS) | Fleet-infra GitOps repo (mirrored by [`example/`](./example/)) |

The base does **not** declare a namespace, hostname, image tag, or
secret values. Those are supplied per env by the overlay. Conversely,
the overlay does not redeclare what the app *is* — it points Flux at
`./kustomize/base` in this repo and patches what differs.

This split has two practical consequences:

1. **Adding a new env is a copy-paste of one overlay directory.** The
   base is unchanged.
2. **Changing app behavior** (a new container, a new ConfigMap key,
   different resource limits) **happens in this repo and reconciles
   into every env on the next Flux poll.** No GitOps-repo edit needed.

### Required external contract

The base expects the overlay to provide three Secrets (full key
contracts in [`kustomize/base/kustomization.yaml`](../../kustomize/base/kustomization.yaml);
full inventory of every Secret the deployment consumes — including
auto-generated ones — in [`secrets.md`](./secrets.md)):

- `minerals-minio-config` — MinIO root credentials, key `config.env`.
- `minerals-s3-creds` — S3 IAM credentials, keys
  `S3_ACCESS_KEY_ID` and `S3_SECRET_ACCESS_KEY`.
- `minerals-pg-app` — auto-generated by CloudNativePG; not committed.

The example provides redacted SealedSecret stubs for the first two. The
third is generated by the CNPG operator at cluster bootstrap and never
appears in any manifest.

---

## How Flux reconciles this app

Flux has two relevant resource kinds in this setup:

1. **`GitRepository` (source-controller)** — polls a git repo and
   caches the working tree on the controller. One per repo, lives in
   `flux-system`. See [`example/flux-source/minerals-repo.yaml`](./example/flux-source/minerals-repo.yaml).

2. **`Kustomization` (kustomize-controller)** — points at a path inside
   a `GitRepository`, runs `kustomize build` against it, applies the
   result into a target namespace, and reconciles drift on a timer.
   One per environment, lives in `flux-system`. See
   [`example/staging/mineral.yaml`](./example/staging/mineral.yaml) and
   [`example/prod/mineral.yaml`](./example/prod/mineral.yaml).

The flow:

```
GitHub (this repo, main)
        │  every 1m
        ▼
GitRepository "minerals-repo"  ─── caches working tree
        │
        ├── Kustomization "mineral-staging"  ── every 10m ──▶  mineral-staging ns
        │       path: ./kustomize/base
        │       images: ghcr.io/.../minerals:staging
        │       patches: ConfigMap ENV=staging
        │
        └── Kustomization "mineral-prod"     ── every 10m ──▶  mineral-prod ns
                path: ./kustomize/base
                images: ghcr.io/.../minerals:prod
                patches: ConfigMap ENV=prod
```

A merge to `main` lands on staging within ~1m (poll) + ~10m (reconcile).
Image rollouts are driven by retagging `staging`/`prod` in GHCR and
Flux re-pulling on the next reconcile (the base sets
`imagePullPolicy: Always`).

---

## Walkthrough: directory layout

```
docs/deploy/
├── README.md                 ← you are here
├── encrypt.md                ← kubeseal workflow
├── secrets.md                ← Secret inventory + new-secret workflow
└── example/
    ├── kustomization.yaml    ← aggregates staging + prod
    ├── flux-source/
    │   └── minerals-repo.yaml      ← GitRepository (flux-system ns)
    ├── staging/
    │   ├── kustomization.yaml
    │   ├── namespace.yaml
    │   ├── certificate.yaml         ← cert-manager cert → web-crt Secret
    │   ├── ingress.yaml             ← v1 Ingress, TLS via web-crt
    │   ├── mineral.yaml             ← Flux Kustomization (image, ENV patch)
    │   ├── minerals-minio-config.yaml  ← SealedSecret (REDACTED)
    │   └── minerals-s3-creds.yaml      ← SealedSecret (REDACTED)
    └── prod/
        └── (same shape as staging)
```

The example mirrors the layout of fleet-infra's
`clusters/new-k3s/mineral/` directory. The flux-source/ subdirectory
is a documentation grouping; in fleet-infra the `GitRepository` lives
under `clusters/<cluster>/flux-system/` alongside other Flux sources.

### Ordering inside an overlay

Each per-env `kustomization.yaml` lists resources in lifecycle order:

1. `namespace.yaml` — must exist before anything else in the env.
2. `certificate.yaml` — cert-manager creates the `web-crt` Secret.
3. `ingress.yaml` — references `web-crt` for TLS.
4. `minerals-minio-config.yaml`, `minerals-s3-creds.yaml` —
   SealedSecrets the base's pods need before they can start.
5. `mineral.yaml` — the Flux `Kustomization` that pulls
   `kustomize/base/` into this namespace.

Flux applies in dependency order regardless of file ordering, but the
explicit ordering reads cleanly and matches what someone would do by
hand.

---

## Per-env differences

Only these things vary between staging and prod in the example:

| Thing                          | staging                              | prod                              |
|--------------------------------|--------------------------------------|-----------------------------------|
| `namespace`                    | `mineral-staging`                    | `mineral-prod`                    |
| `Ingress`/`Certificate` host   | `mineral-staging.dickey.cloud`       | `mineral.dickey.cloud`            |
| `Kustomization.metadata.name`  | `mineral-staging`                    | `mineral-prod`                    |
| `images[0].newTag`             | `staging`                            | `prod`                            |
| ConfigMap `ENV` patch value    | `staging`                            | `prod`                            |
| SealedSecret ciphertext        | encrypted to `mineral-staging` scope | encrypted to `mineral-prod` scope |

Everything else is identical on purpose. Symmetric overlays make
copy-paste-to-add-a-new-env safe.

### The ENV ConfigMap patch is in BOTH overlays

The base's `configmap.yaml` defaults `ENV: "prod"`. A reasonable
shortcut is to omit the patch in the prod overlay since the default is
already correct — that's what fleet-infra currently does, and it's the
**one inconsistency this example deliberately fixes**.

The example keeps the ENV patch present and explicit in *both*
overlays. Two reasons:

- **Shape symmetry** — the two overlay files differ only by literals,
  not by which fields exist. Diffing them or copying one to seed a new
  env doesn't risk dropping a load-bearing patch by accident.
- **No reliance on a base default** — if someone later changes the
  base's ENV default (say, to `dev` for local development), the prod
  overlay still pins prod explicitly and doesn't silently flip.

The cost is a few lines of YAML that look like a no-op. The benefit is
that the overlay's behavior is obvious from the overlay itself.

---

## Bootstrapping a new environment

1. Copy `example/staging/` to a new directory, e.g.
   `clusters/<cluster>/mineral/dev/` in your fleet-infra repo.
2. Edit the namespace, hostnames, Kustomization name, and image tag to
   match the new env.
3. Generate fresh SealedSecrets for the new namespace — see
   [`encrypt.md`](./encrypt.md). The ciphertext is namespace-scoped, so
   you cannot reuse staging's.
4. Add the new directory to the parent `kustomization.yaml`.
5. Open a fleet-infra PR (commits there require human approval).
6. After merge, the cluster's flux-system reconciles the new env on
   its next poll.

---

## Verifying the example renders

```bash
kubectl kustomize docs/deploy/example/staging
kubectl kustomize docs/deploy/example/prod
```

Both should render without error. The output includes the SealedSecret
manifests with placeholder ciphertext (`REPLACE_WITH_KUBESEAL_OUTPUT`)
— that's expected; the SealedSecret CRD doesn't validate ciphertext
shape via kustomize (the controller does that at apply time).

---

## Optional add-ons (deliberately omitted from the example)

The minimal example covers what every env needs. The following are
real options but are not in the example to keep the minimum surface
small:

- **NetworkPolicy** — restrict pod-to-pod traffic. Worth adding once
  the cluster has multiple workloads sharing nodes.
- **PodDisruptionBudget** — only meaningful with `replicas > 1`. v1 is
  single-replica, so a PDB would either be a no-op or block voluntary
  drains.
- **HorizontalPodAutoscaler** — *incompatible with v1*. The base uses
  `strategy: Recreate` with `replicas: 1` because session/file-lock
  semantics for multi-replica are an unsolved v2 question. An HPA
  would race against the single-replica assumption. Add when v2 ships
  multi-replica with a defined consistency story.
- **imagePullSecret** — `ghcr.io/dickeyfpersonalprojects/minerals` is
  currently public, so anonymous pulls work. Add an `imagePullSecrets`
  entry on the Deployment's PodSpec (and the Secret to back it) if the
  repo is made private.

---

## Cross-references

- Secret inventory + new-secret workflow: [`secrets.md`](./secrets.md).
- App contract / non-secret env vars / secret env vars: [`CONTRACT.md`](../../CONTRACT.md) §15.
- IaC layout rule (base in app repo, overlay in GitOps repo): `CONTRACT.md` §2.
- App scratch-filesystem rule (`/tmp` only): `CONTRACT.md` §17.
- Migration init-container pattern: `CONTRACT.md` §6.4.
- Required external Secrets contract: [`kustomize/base/kustomization.yaml`](../../kustomize/base/kustomization.yaml) header.
